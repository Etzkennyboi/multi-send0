{
  "name": "multi_send_token",
  "description": "Batch transfer ERC20 tokens to multiple recipients. Returns a bundle of individual transfer transactions ready for execution.",
  "parameters": {
    "type": "object",
    "properties": {
      "token": {
        "type": "string",
        "description": "The ERC20 token address to send."
      },
      "recipients": {
        "type": "array",
        "description": "List of recipients and corresponding amounts in wei.",
        "minItems": 1,
        "maxItems": 100,
        "items": {
          "type": "object",
          "properties": {
            "address": {
              "type": "string",
              "description": "Recipient wallet address."
            },
            "amount": {
              "type": "string",
              "description": "Amount to send in wei."
            }
          },
          "required": ["address", "amount"]
        }
      },
      "callerAddress": {
        "type": "string",
        "description": "Optional: The address of the sender (required for gas simulation)."
      },
      "simulate": {
        "type": "boolean",
        "description": "Whether to simulate gas estimation per transaction.",
        "default": true
      }
    },
    "required": ["token", "recipients"]
  }
}
